FROM python:3.12-slim

ENV PYTHONUNBUFFERED=1

WORKDIR /app

RUN pip install --no-cache-dir websockets==12.0

COPY deploy/parity-compose /app/parity-compose

CMD ["python", "/app/parity-compose/gateway_mock.py"]
